server:
  port: 8081

spring:
  application:
    name: book-service
  data:
    mongodb:
      uri: ${MONGODB_BARILIBRO_URI}

security:
  jwt:
    secret: ${JWT_SECRET_BARILIBRO}
    expiration: 86400000
