server:
  port: 8083

spring:
  application:
    name: purchase-service
  data:
    mongodb:
      uri: ${MONGODB_BARILIBRO_URI}

security:
  jwt:
    secret: ${JWT_SECRET_BARILIBRO}
    expiration: 86400000
